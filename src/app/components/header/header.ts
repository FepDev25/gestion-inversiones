import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: true,
  imports: [], // Kept for consistency, can be removed if truly no imports
  templateUrl: './header.html',
  styles: ``
})
export class Header {

}
